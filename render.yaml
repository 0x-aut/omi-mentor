services:
  # A Docker web service
  - type: web
    name: omi-mentor-dev
    runtime: python
    plan: free
    autoDeploy: false
    buildCommand: uv add -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port 8000